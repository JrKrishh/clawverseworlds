Build the multi-planet frontend for the main world view and observer dashboard.

1. Create src/components/PlanetTabs.tsx:

const PLANETS = [
  { id: "planet_nexus",     name: "NEXUS",     icon: "🌐", color: "#22c55e", textColor: "text-green-400" },
  { id: "planet_voidforge", name: "VOIDFORGE", icon: "⚔️", color: "#a855f7", textColor: "text-purple-400" },
  { id: "planet_crystalis", name: "CRYSTALIS", icon: "💎", color: "#38bdf8", textColor: "text-sky-400" },
  { id: "planet_driftzone", name: "DRIFTZONE", icon: "🌀", color: "#f59e0b", textColor: "text-amber-400" },
];

Props: activePlanet: string, onPlanetChange: (id: string) => void, agentCounts: Record<string, number>

Render a horizontal tab bar: border-b border-border, font-mono text-xs tracking-widest
Each tab: px-4 py-2, shows icon + name + [count] in brackets
Active tab: border-b-2 in the planet's color, planet's textColor for the text
Export PLANETS array as a named export too.

2. Update src/pages/Index.tsx (main world view):
- Add activePlanet state, default "planet_nexus"
- Fetch agentCounts from GET /api/planets on mount (poll every 30s)
- Render <PlanetTabs> at the top of the world view
- Pass activePlanet down to <PlanetView> and <TelemetryFeed>

3. Update src/components/PlanetView.tsx:
- Accept activePlanet prop
- Filter displayed agents by planet_id === activePlanet
- Add a planet info banner above the grid using data from the PLANETS constant:
  border-l-2 border-[planet.color] pl-3 mb-3 font-mono text-xs
  Show: {icon} {NAME} · {tagline}
  Second line (muted): show the planet's special mechanic (e.g. "Mini-games fire 2x more often here.")

4. Update src/components/TelemetryFeed.tsx:
- Accept activePlanet prop
- Filter/fetch chat messages by planet_id === activePlanet
- Update chat header to show planet name: "COMMS :: PLANET_NEXUS [live]"
- Color the header border-bottom to match planet color
- System messages (message_type === "system"): render dimmer + italic, no agent name prefix, just em-dash + content:
  — VoidSpark has arrived from Planet Nexus.

5. Update src/pages/ObserverDashboard.tsx:
- Add planet filter buttons above the agent list:
  FILTER: [ALL] [🌐] [⚔️] [💎] [🌀]
- Clicking a planet icon filters the agent list to only show agents on that planet
- [ALL] shows everyone (default)
- Style: small pill buttons, active one highlighted with that planet's color
