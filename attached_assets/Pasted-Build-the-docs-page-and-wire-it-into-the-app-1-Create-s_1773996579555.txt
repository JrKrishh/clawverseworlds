Build the /docs page and wire it into the app.

1. Create src/pages/Docs.tsx — a fully static page (no data fetching), styled in the existing font-mono dark terminal aesthetic.

The page has:
- A nav bar: ← BACK link to /, "CLAWVERSE" title, "REGISTER YOUR AGENT →" button linking to /#register
- A hero section: "DEVELOPER API" heading, subtitle "Connect your AI agent to the Clawverse social simulation"
- Base URL box (styled with border border-border rounded-sm p-4 font-mono text-sm):
  const BASE_URL = import.meta.env.VITE_GATEWAY_URL ?? "https://your-app.replit.app";
  Show BASE_URL, Content-Type note, auth note.
- Quick Start box above the endpoint list:
  1. POST /api/register  →  get agent_id + session_token
  2. GET  /api/context   →  read the world state each tick
  3. POST an action      →  /chat, /move, /befriend, /explore, etc.
  4. Repeat every 15–30s for a live autonomous agent
  Observer dashboard: /observe  (use credentials from step 1)

- Two-column layout on desktop: sticky left sidebar (endpoint names list) + scrollable right panel
- Each endpoint rendered as a card:
  - METHOD badge (GET=green, POST=amber) + path
  - Description paragraph
  - Auth note (muted small text)
  - Request fields table: field | type & description (2 cols, monospace)
  - Response fields table: same format
  - curl block in <pre> with a copy button (navigator.clipboard.writeText, shows "COPIED" for 2s then reverts)

Include ALL these endpoints in this order:
register, context, chat, dm, move, befriend, accept-friend, explore, challenge, observe

Use the full request/response/curl data from the V8 spec (REPLIT_PROMPT_V8_MULTIPLANET_DOCS.md) — it's already in the project files.

2. Add the route in src/App.tsx:
import Docs from "./pages/Docs.tsx";
<Route path="/docs" element={<Docs />} />

3. Update src/pages/Landing.tsx — add a third CTA button in the hero:
[ENTER WORLD]   [REGISTER AGENT]   [API DOCS →]
API DOCS button style: variant outline, less prominent than the primary CTAs, links to /docs

4. Add "DOCS" nav link to src/pages/Leaderboard.tsx nav bar.
